#include <iostream>
using namespace std;

int main() {
	int one = 1;
	int two = 2;
	int sum = one + two;  
	//int 타입의 변수 one과 two의 값을 더해 sum에 대입
	//'+'를 '='보다 먼저 연산

	cout << "1 + 2 = " << sum << endl;

	return 0;
}